#ifndef NORMAL_MODE_H
#define NORMAL_MODE_H

/*
 * the normal mode of the program for moving around and switching to all the other modes
 * params:
 * ch: the character inputted by the user
 * returns:
 * void
*/
void normal_mode(int ch);

#endif
