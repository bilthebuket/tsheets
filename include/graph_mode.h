#ifndef GRAPH_MODE_H
#define GRAPH_MODE_H

/*
 * mode for when the user is viewing a graph they generated
 * params:
 * ch: the key the user presses
 * returns:
 * void
*/
void graph_mode(int ch);

#endif
